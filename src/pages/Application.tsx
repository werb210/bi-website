import SharedApplicationFlow from "../components/application/SharedApplicationFlow";

export default function Application() {
  return <SharedApplicationFlow step="application" lenderMode={false} />;
}
